export enum Direction {
  UP = 'up',
  LEFT = 'left',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}

export enum Axis {
  VERTICAL = 'vertical',
  HORIZONTAL = 'horizontal',
}

export enum Section {
  CONTACT = 'contact',
  OFFERINGS = 'offerings',
}
